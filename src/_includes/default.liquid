<!DOCTYPE html>
<html lang="en">
    {% include head %}
    <body class="bg-yellow-50 text-gray-600 min-h-screen relative">
        {% include header %}
        <main class="pt-10" aria-label="Content">
            {{ content }}
        </main>
        {% include footer %}
    </body>
</html>