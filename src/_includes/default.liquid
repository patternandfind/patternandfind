<!DOCTYPE html>
<html lang="en">
    {% include head %}
    <body class="bg-yellow-50 text-gray-600 flex flex-col min-h-screen justify-between">
        {% include header %}
        <hr class="border-0 bg-gray-600 text-gray-600 lg:mt-4 md:mt-4 lg:h-1 md:h-1 sm:h-1 h-px mx-10">
        <main class="mb-auto min-h-10" aria-label="Content">
            {{ content }}
        </main>
        {% include footer %}
    </body>
</html>